<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PickMeal.PickMeal.mapper.UserMapper">
   <select id = "findByid" resultType="PickMeal.PickMeal.domain.User" parameterType="String">
       SELECT user_id, id, password, nickname, name, email, birthDate, gender, phoneNumber, joinDate, socialLoginSite, socialId
       FROM user
       WHERE id = #{id}
   </select>
    <insert id = "save" parameterType="PickMeal.PickMeal.domain.User">
        INSERT INTO user (id, password, nickname, name, email, birthDate, gender, phoneNumber)
        VALUES(#{id}, #{password}, #{nickname}, #{name}, #{email}, #{birthDate}, #{gender}, #{phoneNumber})
    </insert>
</mapper>